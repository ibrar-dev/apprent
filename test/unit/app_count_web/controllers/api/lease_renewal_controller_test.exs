defmodule AppCountWeb.Controllers.API.LeaseRenewalControllerTest do
  use AppCount.Case
  use AppCountWeb.ConnCase
  #  alias AppCount.Accounting
  #  alias AppCount.Leases.Lease
  @moduletag :lease_renewal_controller

  #  setup do
  #    {:ok, lease: insert(:lease), admin: insert(:admin)}
  #  end
  #
  #  test "index", %{conn: conn, admin: admin} do
  #
  #  end
end

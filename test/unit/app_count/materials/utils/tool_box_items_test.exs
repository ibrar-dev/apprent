defmodule AppCount.Materials.ToolBoxItemTest do
  use AppCount.Case
  #  import AppCount.{Factory}
  #  alias AppCount.Materials.Utils.ToolboxItems

  #  setup do
  ##    now = AppCount.current_time()
  ##    start_date = Timex.shift(now, days: -2)
  ##    end_date = Timex.shift(now, days: 30)
  #    tech = insert(:tech)
  #    stock = insert(:stock)
  #  end

  #  test "list_moved_items", %{start_date: start_date, end_date: end_date} do
  #    toolbox_item = ToolboxItems.list_moved_items(start_date, end_date)
  ##    assert String.length
  #  end
  #
  #  test "list_all_items" do
  #    toolbox_items = ToolboxItems.list_all_items()
  ##    assert Repo.aggregate(ToolboxItems) == Map.size(toolbox_items)
  #  end
  #
  #  test "checkout_items_in_toolbox", %{tech: tech, stock: stock} do
  #    ToolboxItems.checkout_items_in_toolbox(tech.id, stock.id)
  #  end
end

defmodule AppCount.Accounting.Ledger.NSF do
  #  import Ecto.Query

  #  def payment_nsfs(property_id, %{"receivable" => receivable, "cash" => cash}, "accrual") do
  #
  #  end
  #
  #  def payment_nsfs(property_id, %{"receivable" => receivable, "cash" => cash}, "cash") do
  #
  #  end
end
